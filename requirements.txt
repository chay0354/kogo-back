Django==4.2.11
djangorestframework==3.14.0
django-cors-headers==4.3.1
psycopg2-binary==2.9.9
python-decouple==3.8
gunicorn==21.2.0
dj-database-url==2.1.0

# Celery for background tasks and scheduling
celery==5.3.6
redis==5.0.1
django-celery-beat==2.5.0  # Optional: manage periodic tasks via Django admin

requests==2.31.0

# Rental agreement PDF generation (pure-Python, no OS-level deps — safe on
# both Fly.io and Vercel serverless, unlike an HTML/CSS renderer)
reportlab==5.0.0
python-bidi==0.6.11

# Reading the previous invoicing software's .xls export (BIFF, not xlsx).
# Pure Python, no OS-level deps.
xlrd==2.0.1

# Secured electronic signature on the fiscal PDFs (apps/documents/signing):
# pyHanko signs PAdES and validates; cryptography and asn1crypto are used
# directly too (the KMS client's JWT, the self-issued certificate). The key
# itself is Cloud KMS's, reached over plain HTTPS through requests.
pyhanko==0.37.0
pyhanko-certvalidator==0.32.1
cryptography==50.0.1
asn1crypto==1.5.1
